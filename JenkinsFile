@Library('javahome-demo') _

pipeline{
    agent any
    stages{
        stage('Demo'){
            steps{
                welcome("Vincenzo")
                script{
                    calculator.add(4,4)
                    calculator.mul(4,5)
                    def data = readFile(file: 'test.txt')
                    println(data)
                }
            }
        }
    }
}

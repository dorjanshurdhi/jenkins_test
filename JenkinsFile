@Library('javahome-demo') _

pipeline{
    agent any
    stages{
        stage('Demo'){
            steps{
                welcome("Elvis")
                script{
                    calculator.add(10,10)
                    calculator.mul(10,2)
                }
            }
        }
    }
}
